@model IEnumerable<animal_hotel.Models.rezerwacje>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.id_data)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.id_zwierz)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.id_data)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.id_zwierz)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.id_rezerwacji">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.id_rezerwacji">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.id_rezerwacji">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
